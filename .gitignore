node_modules
dist
.env
.env.local
.env.bot

# Cache do Puppeteer/Chrome (instalado localmente para dev, não vai para o servidor)
.cache

# Sessão do WhatsApp (gerada em runtime, não versionar)
.wwebjs_auth
.wwebjs_cache
